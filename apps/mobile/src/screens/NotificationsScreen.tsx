import { ScreenPlaceholder } from '../components/ScreenPlaceholder';

export function NotificationsScreen() {
  return (
    <ScreenPlaceholder
      title="Notifications"
      description="See reminders, customer activity and items that need your approval."
    />
  );
}
