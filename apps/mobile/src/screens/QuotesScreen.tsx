import { ScreenPlaceholder } from '../components/ScreenPlaceholder';

export function QuotesScreen() {
  return (
    <ScreenPlaceholder
      title="Quotes"
      description="Prepare professional quote drafts and send them only after confirmation."
    />
  );
}
