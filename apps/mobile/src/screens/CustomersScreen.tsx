import { ScreenPlaceholder } from '../components/ScreenPlaceholder';

export function CustomersScreen() {
  return (
    <ScreenPlaceholder
      title="Customers"
      description="Keep contact details, properties, job history and conversations together."
    />
  );
}
