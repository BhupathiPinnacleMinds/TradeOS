import { ScreenPlaceholder } from '../components/ScreenPlaceholder';

export function SettingsScreen() {
  return (
    <ScreenPlaceholder
      title="Settings"
      description="Manage your business workspace, members, defaults and integrations."
    />
  );
}
