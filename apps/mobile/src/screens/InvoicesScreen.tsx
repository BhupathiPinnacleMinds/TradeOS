import { ScreenPlaceholder } from '../components/ScreenPlaceholder';

export function InvoicesScreen() {
  return (
    <ScreenPlaceholder
      title="Invoices"
      description="Track drafts, amounts due, payments and follow-up reminders."
    />
  );
}
