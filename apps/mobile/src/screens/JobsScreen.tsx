import { ScreenPlaceholder } from '../components/ScreenPlaceholder';

export function JobsScreen() {
  return (
    <ScreenPlaceholder
      title="Jobs"
      description="Plan upcoming work and track every job from lead through completion."
    />
  );
}
